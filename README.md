> instalation

```shell
sudo pacman -Syu
sudo pacman -S --needed git github-cli
cd ~
mkdir gitTrash
cd gitTrash
gh auth login
git clone https://github.com/Evrandil/Lapek --branch hyprland
cd Lapek
bash install.sh
```
